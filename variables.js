let myCat = "zaytoon";  //declaring and intializing a variable
myCat = "bobi";
let myAge = 24;

let dolphinGoodbye = "So long and thanks for all the fish";
let iAmAlive = true;
let test = 11 > 3;

let myNameList = ["milky", "bopti", "shilem"];
let myAgeList = [20, 12, 35];
myNameArray[0]; // should return 'milky'
myNumberArray[2]; // should return 40

let student = {name: "milky" , age: 20 , occupation: "a collage student" };  //object
 student.name; //to retrive information from the object.

 let myNumber = "500"; 
typeof myNumber; // returns a string
myNumber = 500; 
typeof myNumber; //returns a number data type

const count = 1; // declare and intialize at the same line

const bird = { species: "Kestrel" };
console.log(bird.species); // "Kestrel"

bird.species = "Striated Caracara";
console.log(bird.species); // "Striated Caracara"

let person = "John Doe", carName = "Volvo", price = 200;

let man = "John Doe",
nameCar = "Volvo",
pricee = 200;

var carame = "Volvo";
var carame;   //valid but not valid for const

let x = "John" + " " + "Doe"; // concatenation of string

let $ = "Hello World";
let $$$ = 2;
let $myMoney = 5;


let x = 250;
    let y = 40.5;
    console.log("Value of x=" + x);
    console.log("Value of y=" + y);

    let str = "hello mey";
    let str1 = "how is life treating you?";
    console.log("value of str=" + str);
    console.log("value of str1=" + str1)
    
    // bigint = operation on large numbers
    let bigNum = 123422222222222222222222222222222222222n
console.log(bigNum);

// create a generic object
let obj = new Object();
// user defined object
let myCatt = new Cat();

// Creating object with the name person
let personn = {
    firstName: "hikma",
    lastName: "tsegaye",
};

// Print the value of object on console
console.log(person.firstName  + "  " + person.lastName);

// Call it with numeric argument
let a = new Array();
    let b = new Array(10);
    let d = new Array(1, 2, 3, "Hello");
    console.log("value of a=" + a);
    console.log("value of b" + b);
    console.log("value of d=" + d);

    if (choice === "sunny" && temperature < 86) {
        para.textContent = `It is ${temperature} degrees outside — nice and sunny. Let's go out to the beach, or the park, and get an ice cream.`;
      } else if (choice === "sunny" && temperature >= 86) {
        para.textContent = `It is ${temperature} degrees outside — REALLY HOT! If you want to go outside, make sure to put some sunscreen on.`;
      }

      if (iceCreamVanOutside || houseStatus === "on fire") {
        console.log("You should leave the house quickly.");
      } else {
        console.log("Probably should just stay in then.");
      }

      if (!(iceCreamVanOutside || houseStatus === "on fire")) {
        console.log("Probably should just stay in then.");
      } else {
        console.log("You should leave the house quickly.");
      }

      